Ejecuta el build de producción del proyecto.

1. Ejecuta `npm run build`
2. Si hay errores, analízalos y propón una corrección
3. Si el build fue exitoso, muestra un resumen del tamaño de los archivos generados en `dist/`
