Inicia el servidor de desarrollo de Vite para este proyecto Vue 3.

Ejecuta `npm run dev` y confirma que el servidor arrancó correctamente.
Si hay errores de dependencias, ejecuta `npm install` primero.
